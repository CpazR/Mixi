namespace Mixi.MidiController;

/**
 * Define the <see cref="InputType"/> and <see cref="ActionType"/> of a given input 
 */
public record InputDefinition(InputType Type, InputActionType ActionType);
